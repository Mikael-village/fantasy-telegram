# ⚔️ Fantasy Dashboard — Telegram Mini App

RPG-дашборд в стиле Diablo II/III для AI-ассистента.

## 📁 Структура

```
fantasy-telegram/
├── bot.py           # Telegram бот
├── server.py        # FastAPI сервер (API + Mini App)
├── index.html       # Mini App фронтенд
├── data.json        # Данные персонажа
├── requirements.txt # Python зависимости
└── README.md        # Эта инструкция
```

## 🚀 Быстрый старт

### 1. Создай бота

1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь `/newbot`
3. Выбери имя и username
4. Скопируй токен (например: `123456789:ABCdef...`)

### 2. Установи зависимости

```bash
cd apps/fantasy-telegram
pip install -r requirements.txt
```

### 3. Настрой переменные окружения

Создай файл `.env`:

```env
BOT_TOKEN=твой_токен_от_botfather
WEBAPP_URL=https://твой-домен.com
API_SECRET=твой_секретный_ключ
```

Или экспортируй в терминале:

```bash
export BOT_TOKEN="123456789:ABCdef..."
export WEBAPP_URL="https://твой-домен.com"
export API_SECRET="random-secret-string"
```

### 4. Запусти локально (для теста)

**Терминал 1 — Сервер:**
```bash
python server.py
# Запустится на http://localhost:8000
```

**Терминал 2 — Бот:**
```bash
python bot.py
```

⚠️ **Для Mini App нужен HTTPS!** Локально можно протестировать только API.

## 🌐 Деплой на хостинг

### Вариант A: Railway.app (рекомендуется)

1. Зарегистрируйся на [railway.app](https://railway.app)
2. Создай новый проект → Deploy from GitHub
3. Добавь переменные окружения:
   - `BOT_TOKEN`
   - `WEBAPP_URL` = URL твоего Railway проекта
   - `API_SECRET`
4. Railway автоматически:
   - Определит Python проект
   - Установит зависимости
   - Даст HTTPS домен

### Вариант B: Render.com

1. Зарегистрируйся на [render.com](https://render.com)
2. New → Web Service
3. Укажи Start Command: `uvicorn server:app --host 0.0.0.0 --port $PORT`
4. Добавь Environment Variables

### Вариант C: VPS + nginx

1. Установи Python 3.10+
2. Установи nginx
3. Получи SSL сертификат через Let's Encrypt:
   ```bash
   certbot --nginx -d твой-домен.com
   ```
4. Настрой nginx как reverse proxy на порт 8000
5. Запусти через systemd или pm2

## 📱 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие + кнопка Mini App |
| `/dashboard` | Открыть дашборд |
| `/status` | Текущее состояние персонажа |
| `/quest <название>` | Добавить новый квест |
| `/done <название>` | Завершить квест (+1 XP) |
| `/hp <число>` | Установить HP |
| `/mana <число>` | Установить использованный контекст |
| `/level` | Повысить уровень |
| `/addknowledge <название>` | Добавить свиток знаний |
| `/addspell название\|⚡\|категория\|5\|описание` | Добавить заклинание |

## 🔌 API

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/` | GET | Mini App (index.html) |
| `/dashboard` | GET | Alias для Mini App |
| `/api/data` | GET | Получить данные персонажа |
| `/api/data` | POST | Обновить данные (требует Authorization: Bearer TOKEN) |
| `/api/health` | GET | Health check |

## 🎮 Как использовать

1. **Начни чат** с ботом → `/start`
2. **Нажми кнопку** "⚔️ Fantasy Dashboard"
3. Откроется **Mini App** прямо в Telegram
4. Смотри статы, заклинания, квесты
5. **Управляй через команды** бота

## 🔄 Актуализация данных

Напиши боту:
```
Актуализируй данные в Fantasy интерфейсе
```

Или используй команды `/hp`, `/mana`, `/quest`, `/done` и т.д.

## 📊 Категории заклинаний

- **Боевая магия** — основные навыки
- **Артефакты** — инструменты
- **Аура поддержки** — soft skills

## 🛠️ Разработка

Для локального тестирования Mini App без HTTPS:

1. Установи [ngrok](https://ngrok.com)
2. Запусти: `ngrok http 8000`
3. Скопируй HTTPS URL от ngrok
4. Обнови `WEBAPP_URL` в `.env`
5. Перезапусти бота

---

*Создано для Помощника Микаела • 2026*
